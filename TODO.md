# TODO

- [ ] Split routes in `client/src/App.tsx` using `React.lazy()` + `Suspense` for HIGH + MEDIUM impact routes.
- [ ] Run `client build` and capture before/after output + bundle warning status.

